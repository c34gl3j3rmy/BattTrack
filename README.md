# Battery Reminder

Version v0.2.

## Changements v0.2

- Correction du statut rouge après une première recharge à 100 %.
- Le mode "LEDs fixes + clignotantes" permet maintenant toutes les LEDs fixes.
- Le pourcentage manuel met à jour l'aperçu LED.
- Export JSON et création batterie déplacés dans le menu hamburger.
- Le bouton + sert à ajouter une mesure, puis à sélectionner la batterie.
- Formulaire batterie simplifié avec "Mode de saisie".
- Configuration LED visuelle par slider.
